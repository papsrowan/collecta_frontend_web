# Configuration Railway - Frontend Collecta (Next.js)
# Utilisé lorsque le service a pour Root Directory ce dossier.

[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
